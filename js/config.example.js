/**
 * SANITY CONFIG — EXAMPLE
 *
 * 1. Copy this file to js/config.js
 * 2. Fill in your Sanity token (Editor role)
 * 3. js/config.js is gitignored — never commit it
 *
 * Get your token at: sanity.io/manage → your project → API → Tokens
 */
window.SanityConfig = {
  projectId: 'sm9yflxc',
  dataset: 'production',
  token: 'sk...',
  apiVersion: '2023-08-01',
  docType: 'tribute',
  approvedOnly: true,
};
